pragma ComponentBehavior: Bound

import qs.components.controls as Controls

// The single implementation lives in qs.components.controls.SliderRow; this
// is the Nexus-sized preset so pages here keep their spacious layout without
// each one opting in.
Controls.SliderRow {
    spacious: true
}
